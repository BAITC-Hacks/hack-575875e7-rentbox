# <Название проекта>

<!--
Шаблон README под требования HackAlem AI, редакция Положения от 23.09.2026:
  п. 5.4.15 / 5.6.4 — обязательный состав README (8 блоков: назначение, архитектура, технологии,
                      установка, запуск, зависимости, параметры окружения, проверка основного сценария);
  п. 5.4.16 / 5.6.5 — если проект не запускается по этим инструкциям, команда НЕ допускается
                      к дальнейшему отбору; пояснения и исправления не принимаются;
  п. 5.6.6          — ключевая функциональность должна проверяться без личных аккаунтов и подписок участников;
  п. 5.4.4/5.4.4.2  — раскрытие сторонних материалов и заранее подготовленных компонентов;
  п. 5.4.12         — раскрытие использованных AI-инструментов;
  п. 5.4.7 / 4.6    — эксперты и жюри проверяют фактический вклад каждого участника.
Все команды должны копироваться и работать из чистого клона. Удалить этот комментарий перед сдачей.
-->

**Трек:** 01 Энергетика · **Задача:** <название задачи> · **Команда:** <название> · **Хакатон:** HackAlem AI, 23.09.2026, Астана

## 1. Что это и какую проблему решает

<2–3 абзаца: проблема (с цифрами), для кого, что делает решение, какой результат получает пользователь.>

## 2. Соответствие требованиям задачи

| # | Требование задачи | Реализовано | Где (файл / модуль / эндпоинт) | Как проверить |
|---|---|---|---|---|
| 1 | … | ✅ | `src/...` | шаг 7.2 ниже |
| 2 | … | ✅ | … | … |
| 3 | … | ⚠️ частично | … | … |

<!-- Требования берутся дословно из ТЗ задачи трека: в Положении критериев техоценки больше нет (п. 5.5.1–5.5.3). -->

## 3. Что реализовано / что не реализовано, известные ограничения

**Реализовано:**
- …

**Не реализовано / ограничения (честно):**
- …
- <если что-то замокано — сказать прямо и пометить в коде `# MOCK:`>

## 4. Архитектура

```
<схема: компоненты и стрелки — источник данных → обработка → модель/логика → API → UI>
```

<Описание каждого компонента и как они связаны. 1 абзац на компонент.>

## 5. Технологии, модели и данные

| Слой | Что используется | Версия | Лицензия |
|---|---|---|---|
| Язык / рантайм | | | |
| Фреймворк | | | |
| Модели (LLM/ML) | | | |
| Данные | <источник, ссылка> | | |
| Хранилище | | | |

## 6. Системные требования

- ОС: …
- Python/Node …: версия
- Docker ≥ … (если используется)
- RAM/CPU/GPU: … <GPU не требовать — у проверяющих его может не быть>
- Доступ в интернет: нужен/не нужен; какие внешние сервисы

## 7. Установка и запуск

### 7.0 Быстрый старт (одна команда)

```bash
git clone <repo> && cd <repo>
cp .env.example .env        # заполнять НЕ обязательно, см. раздел 7.3
docker compose up --build   # или: make run / ./run.sh
```

Открыть: http://localhost:<port>

### 7.1 Установка без Docker

```bash
<пошагово, каждая команда копируется>
```

### 7.2 Проверка основного сценария

1. <Что сделать: открыть/вызвать/загрузить файл `data/sample/...`>
2. <Что ввести>
3. <Что должно получиться — с примером вывода/скриншотом>

```bash
# Пример запроса
curl -X POST http://localhost:<port>/api/... -d @data/sample/request.json
# Ожидаемый ответ
{ ... }
```

### 7.3 Проверка без собственных ключей и аккаунтов (п. 5.6.6)

<!--
Положение прямо требует, чтобы ключевую функциональность можно было проверить БЕЗ личных аккаунтов
участников, персональных подписок и закрытых учётных записей. Описать здесь один из вариантов:
демо-ключ/тестовая учётка, режим работы без внешнего API, или локальная модель.
-->

- Проект запускается **без заполнения `.env`**: <что происходит — демо-режим на данных `data/sample/`, локальная логика, …>
- Что именно работает в этом режиме: <перечислить>
- Что требует внешнего ключа и как это выглядит без него: <понятное сообщение об ошибке, не traceback>
- Демонстрационный доступ / тестовая учётная запись: <если предоставляется — как получить>

### 7.4 Тесты

```bash
<команда запуска smoke-тестов>
```

## 8. Конфигурация и переменные окружения

| Переменная | Обязательна | Назначение | Что будет, если не задать | Пример / где взять |
|---|---|---|---|---|
| `NVIDIA_API_KEY` | нет | … | работает демо-режим (см. 7.3) | https://build.nvidia.com |
| `…` | | | | |

Файл `.env.example` содержит все переменные с комментариями. Секреты в репозиторий не коммитятся.

## 9. Демо

- Развёрнутая версия: <URL или «нет»>
- Видео демо: <ссылка или «нет»>

## 10. Внешние материалы (раскрытие по п. 5.4.4 Положения)

| Тип | Что | Откуда | Лицензия | Как используется |
|---|---|---|---|---|
| Библиотека | | | | |
| Открытая модель | | | | |
| Датасет | | | | |
| Шаблон / boilerplate | | | | |

## 11. Заранее подготовленные компоненты (п. 5.4.4.2)

<!--
Положение разрешает приносить с собой технические компоненты, инструменты, собственные библиотеки,
шаблоны и инфраструктуру, если они НЕ являются готовым продуктом и НЕ составляют основную часть решения.
Основная функциональность под задачу должна быть написана 23.09 в 13:00–18:00. Перечислить честно.
-->

| Что | Когда создано | Роль в проекте | Почему это не основная функциональность |
|---|---|---|---|
| <шаблон/каркас> | до хакатона | каркас API и Docker-обвязка | бизнес-логика задачи написана в соревновательной части |

## 12. AI-инструменты, использованные при разработке (п. 5.4.12)

- OpenAI Codex — <как использовался: генерация модулей X, тесты, README…>
- <другие: Claude Code, Copilot, ChatGPT …>

## 13. Команда и вклад

| Участник | Роль | Вклад |
|---|---|---|
| <имя> (капитан) | | |
| <имя> | | |
| <имя> | | |

## 14. Что дальше (roadmap)

- …
