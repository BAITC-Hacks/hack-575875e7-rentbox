# Старт 23 сентября — что делать по шагам

Рабочая машина — **MacBook Air M5, macOS, Apple Silicon (`arm64`)**, оболочка `zsh`. Наличие и версии инструментов нужно проверить на этом Mac. Ниже — порядок действий на площадке.

**Тайминг дня** (п. 3.5, 5.3.1): check-in **09:00–12:00** · открытие **12:00–13:00** · разработка **13:00–18:00** · закрытие 18:00–18:30.
Итоговая версия проекта = содержимое репозитория на **18:00**, независимо от момента блокировки интерфейса платформы (п. 5.4.13).

> 🔴 Положение переписано 23.09.2026: 6 разделов вместо 17, вся нумерация изменилась, **критерии технической оценки
> вынесены из Положения в техзадание задачи** (п. 5.5.1–5.5.3). Подробности — `docs/hackathon/cheatsheet.md`, §11.

## За 10 минут до старта

```bash
cd /Users/romariodev/Desktop/hack-575875e7-rentbox
git status --short
```

Если для проекта выбран Docker, запустить установленный Docker Desktop для Mac и проверить `docker info`. При использовании NVIDIA API и настроенном ключе выполнить `bash scripts/test-nvidia-api.sh`.

Проверить с собой: ноутбук, зарядка, **LAN-переходник**, **персональный номер участника** (приходит через официальный Telegram-бот, п. 5.2.3) у каждого, документ, телефон из заявки. Номер передавать нельзя — это основание для отказа в допуске (п. 5.2.6).

## Шаг 1. Получить задачу и репозиторий

1. Залогиниться на edu.astanahub.com → вкладка «Треки» → трек 01 «Энергетика».
2. **Скопировать текст задачи** в `docs/hackathon/track-energy.md` (там готовая структура) — вместе с **критериями оценки и весами баллов**, обязательными требованиями, форматом данных, требованиями к инструментам, способом проверки.
   ⚠️ Это теперь критично: в Положении критериев техотбора больше нет, единственный источник — ТЗ задачи (п. 5.5.1–5.5.3). Применяются только критерии, опубликованные **до 13:00** (п. 5.5.3).
3. Найти и сохранить в `docs/hackathon/`: порядок отбора финалистов и их количество (п. 5.6.7), условия призов (п. 5.8.2), **точное время завершения приёма проектов** (п. 5.4.2).
4. Получить GitHub-репозиторий, созданный платформой, и склонировать его. **Вся разработка только там** (п. 5.4.9, 5.4.11) — иначе дисквалификация (п. 5.9.2).

## Шаг 2. Первый коммит — сразу

Документация и скрипты уже находятся в текущем клоне. Зафиксировать их:

```bash
cd /Users/romariodev/Desktop/hack-575875e7-rentbox
git add AGENTS.md CLAUDE.md START-HERE.md README.md docs scripts
git commit -m "документация хакатона и служебные скрипты"
git push
```

Так Codex сразу увидит правила хакатона (`AGENTS.md`), а в репозитории с первой минуты есть подтверждаемая история (п. 5.4.8, 5.4.11).

Шаблон — это «заранее подготовленный технический компонент» по п. 5.4.4.2: использовать можно, но **раскрыть в README** (раздел «Заранее подготовленные компоненты») и написать основную функциональность под задачу в соревновательной части.

## Шаг 3. Включить почасовые коммиты

```bash
./scripts/hourly-commit.sh --watch &        # напоминание каждые 50 минут
```

Каждый час: `./scripts/hourly-commit.sh "что сделали"`. Отсутствие подтверждённого результата за любой отчётный час — основание для дисквалификации (п. 5.4.8, 5.9.2). Засчитываются не только коммиты кода: прототип, дизайн, схема архитектуры, настроенная модель, тестирование, подготовленные данные (п. 5.4.8).

## Шаг 4. Выбрать стек и подготовить каркас

Корневой папки `templates/` в текущем клоне нет. Стек выбираем после получения ТЗ; возможные варианты:

| Стек | Когда брать |
|---|---|
| FastAPI + Vite/React | нужен API и дашборд для Demo Day |
| Streamlit + Plotly | нужен быстрый дашборд |
| Next.js + TypeScript + Tailwind | нужен интерфейс и логика на TypeScript |

Подготовить каркас с зависимостями, совместимыми с Apple Silicon / `arm64`, инструкцией запуска и `.env.example`, если нужны параметры окружения. Доступный шаблон README — `docs/hackathon/templates/README-template.md`. Готовность каркаса и запуск проверить после его добавления.

## Шаг 5. Работа

Приоритеты (из `docs/hackathon/scoring.md`):

1. **P0** — выписать обязательные требования и критерии из ТЗ задачи, закрыть их в первую очередь
2. **P0** — один сквозной рабочий сценарий «вход → результат»
3. **P0** — коммит каждый час (п. 5.4.8), README обновляется по ходу
4. **P0** — запуск одной командой из чистого клона и проверяемость **без личных ключей** (п. 5.4.16, 5.6.6)
5. **P1** — валидация ввода, обработка ошибок, smoke-тесты
6. **P2** — деплой, схема архитектуры, слайды

Лучше один полностью рабочий сценарий, чем три полурабочих — у нас ровно 5 часов, 13:00–18:00.

**Два пункта, по которым снимают без обсуждения:**
- **п. 5.4.16 / 5.6.5** — не запустилось по инструкциям из репозитория → команда не допускается к дальнейшему отбору, **пояснения и исправления не принимаются**. (Раньше можно было пояснить — теперь нет.)
- **п. 5.6.6** — ключевую функциональность должно быть можно проверить **без личных аккаунтов и подписок**. Нужен демо-доступ, тестовая учётка или рабочий режим без ключа.

## Шаг 6. 17:30 — feature freeze

```bash
./scripts/check-clean-env.sh     # клонирует репо в чистую папку и поднимает через Docker
```

Скрипт проверяет ровно то, что будут делать технические эксперты (п. 5.4.16, 5.6.3): наличие README, `.env.example`, способа запуска одной командой, отсутствие `.env` в репозитории и фактический `docker compose up`.

Дальше: заполнить в README разделы 1–3 и 10–14 → финальный коммит и **push до 18:00**. Конкурсная версия — то, что в репозитории на 18:00 (п. 5.4.13); незапушенное не существует, изменения после не учитываются даже на Demo Day (п. 5.4.14).

---

## Текущее окружение

| Компонент | Что известно |
|---|---|
| Компьютер | MacBook Air M5 — указан пользователем |
| ОС и архитектура | macOS, Apple Silicon / `arm64`; `uname -sm` возвращает `Darwin arm64` |
| Оболочка | `zsh` |
| Python, Node.js, npm, Docker и другие инструменты | Наличие и версии ещё не проверены на этом Mac |
| Окружение Python и каркас приложения | На 23.09.2026 в клоне нет `.venv` и корневой папки `templates/` |
| Скиллы | Доступность проверять по каталогу текущей сессии |

Перед настройкой проекта проверить нужные инструменты и Git-авторство на этой машине. Фиксировать фактические версии зависимостей и способ запуска после выбора стека.
