# ChangeLog

Журнал изменений проекта. Ведётся для передачи контекста между агентами (Codex, Claude Code)
и участниками команды: что уже сделано, какие интерфейсы доступны, что нельзя ломать.

**Как пользоваться, если вы агент и только подключились к проекту:**

1. Прочитайте раздел «Текущее состояние» — он описывает, что в репозитории есть прямо сейчас.
2. Найдите свою область в «Интерфейсах» — там сигнатуры и примеры, чтобы не изобретать заново.
3. Перед изменением чужого модуля посмотрите «Решения и ограничения» — там причины, по которым сделано именно так.
4. **После своих изменений добавьте запись в начало «Хронологии»** в том же формате. Коротко: что, зачем, что это меняет для других.

Проект: кейс «Agentic AI для прогнозирования выработки ВЭС», трек 01 «Энергетика», HackAlem AI 23.09.2026.
Текст задачи и разбор — в заметках команды (вне этого репозитория).

---

## Текущее состояние

```
data/incoming/        turbine_1.csv, turbine_2.csv — исходные CSV от организаторов (в репозитории)
data/forecasts.duckdb генерируется, в .gitignore — история прогнозов агента
docs/PLAN.md          порядок работ и зоны ответственности
docs/BACKLOG.md       общий бэклог проекта и приоритеты команды
scripts/audit_data.py аудит исходных данных без изменения значений
reports/data-audit.*  отчёт аудита: .md для людей, .json с SHA-256 для машин
src/storage.py        хранилище истории прогнозов на DuckDB
src/data.py           полные часовые средние; исходный часовой пояс не назначается
src/weather.py        архив GFS/ICON, кэш и политика доступности as_of
src/model.py          регрессоры, экспорт MLP на NumPy, predict_weather
scripts/train_*.py    обучение кривой мощности и прогнозной модели
scripts/search_models.py поиск на CPU/RTX 5090/Brev
scripts/predict.py    запуск модели без сети, ключей и GPU
artifacts/forecast/  модель, январская оценка, прогнозы февраля
artifacts/gfs-model/ основной ансамбль пяти MLP: январская MAE 0.15947, CPU-инференс
data/gfs-runs/       730 операционных циклов NOAA с данными о публикации
src/agent.py         контроллер policy: погода → модель → анализ → ревизии
data/weather/        ответы погодного API и SHA-256
tests/test_storage.py 10 тестов хранилища
docs/PLAN.md          план работ и разделение ответственности
docs/API.md           контракт API для фронта
src/api.py           единая точка входа HTTP API
backend/app/         backend дашборда: очередь, состояния, выдача, CSV и replay
backend/app/prompts/ системный промпт ASTRA и справка помощника Windcast
src/nvidia_api.py    необязательные пояснения NVIDIA NIM, без изменения прогноза
```

Основная модель NOAA обучена на CPU, RTX 5090 и Brev A6000 (400 конфигураций).
Агент подключён к API и DuckDB; новый replay завершил все 29 ежедневных выпусков
февраля без ошибок. Frontend подключён к HTTP API: реальные прогнозы, события, CSV и история запусков.
Часовой пояс и смысл меток CSV пока исследовательские; публикация использованных
объектов NOAA проверяется относительно каждого `as_of`.

---

## Интерфейсы

### Прогнозная модель — готовый CPU-инференс

```python
import joblib
from src.gfs_weather import select_run
from src.gfs_model import predict

bundle = joblib.load("artifacts/gfs-model/model.joblib")
weather, manifest = select_run("2026-01-31T18:00:00Z")
power = predict(bundle, weather)  # вектор, те же строки, [0, 1]
```

Один вызов возвращает погоду двух турбин: 96 строк для 48 ч. Проверяется
граница доступности погоды и конец обучения относительно `as_of`.
Артефакт загружать только из доверенного источника.

**Для фронта:** основной ансамбль NOAA на январе — MAE 0.15947, RMSE 0.23190;
1–24 ч MAE 0.15226, 25–48 ч MAE 0.16692. Это доли нормализованной мощности,
не MAPE. Факта февраля нет. Метрики `artifacts/power_curve` с фактическим
ветром не брать на график качества прогноза.

**Временные допущения:** для исследовательского обучения явно заданы UTC+5,
метка начала 10 минут, выпуск 23:00 по часам CSV. Это не ответ организаторов.
В артефакте сохранено `time_assumptions.confirmed_by_organizers=false`.

**Интеграция погоды с API v0.3:** основная модель NOAA возвращает `single_run`
с `initialization_time` и `available_at`, полученными из GRIB и S3.
Пример: `reports/agent-noaa-first-forecast.json`.
Для предыдущей исследовательской модели Previous Runs использует фиксированные offsets.
Неизвестные `initialization_time`/`available_at` равны null. Поле модуля погоды
`available_at_upper_bound = valid_time - offset_days*24h + 12h` передаётся
в API как `available_at_estimate`; backend перепроверяет формулу и `<= as_of`.
Происхождение GFS/ICON хранится для каждого часа через `weather.sources` и
`points[].weather_inputs`. Политика +12 часов явно отмечается как допущение.
Описание: `docs/backend/weather-provenance.md`. Кэш содержит реальные ответы,
параметры и хэши.

### `src/storage.py` — история прогнозов

Одна DuckDB-база в файле, без сервера. Хранит **каждую** попытку спрогнозировать час, а не только последнюю:
агент возвращается к одному и тому же часу на горизонте 48 ч, потом 24 ч, потом при обновлении погоды.

```python
from datetime import datetime
from src.storage import ForecastStore, ForecastRow

with ForecastStore("data/forecasts.duckdb") as store:
    run_id = store.record_run(
        issued_at=datetime(2026, 1, 31, 12, 0),   # симулируемое «сейчас», не реальное время
        turbine_id=1,                              # только 1 или 2
        weather_source="open-meteo historical-forecast",
        weather_run="previous_day1",               # какой выпуск прогноза использован
        model_name="hist-gradient-boosting",
        model_version="v1",
        horizon_hours=48,
        trigger="scheduled",                       # scheduled | weather_update | backfill | manual
    )
    store.record_forecast(run_id, [
        ForecastRow(datetime(2026, 2, 1, 0, 0), 0.42, wind_speed=7.1, temperature=-3.0),
    ])
```

Чтение:

| Метод | Что возвращает |
|---|---|
| `store.latest(turbine_id=None)` | прогноз, который сейчас «в силе» для каждого часа (побеждает более поздний `issued_at`) |
| `store.revisions(turbine_id, target_ts)` | все прогнозы на один час по порядку, с колонкой `change` — насколько изменился |
| `store.accuracy_by_lead()` | MAE, RMSE, bias по горизонтам — только по часам, где есть факт |
| `store.summary()` | счётчики строк, для быстрой проверки |

Все возвращают `pandas.DataFrame`.

SQL-представления в базе, если удобнее запросом: `latest_forecasts`, `forecast_revisions`, `forecast_accuracy`.

CLI:

```bash
python -m src.storage init       # создать схему (единственная команда, создающая файл)
python -m src.storage summary    # счётчики строк
python -m src.storage accuracy   # ошибки по горизонтам
```

Ошибки — `StorageError` с текстом, объясняющим, что не так, вместо traceback драйвера.

### `scripts/audit_data.py` — аудит исходных данных

```bash
python scripts/audit_data.py --input data/incoming --output reports/data-audit.json
```

Только читает и описывает: не заполняет пропуски, не назначает часовой пояс, не обучает.

---

## Решения и ограничения

**Почему DuckDB, а не Postgres.** Данных мало (два CSV по 6 МБ), а каждый сетевой сервис — риск, что проект
не поднимется у проверяющего. БД лежит одним файлом, поднимать нечего.

**Почему БД не в git.** `data/*.duckdb` в `.gitignore`: файл бинарный и генерируемый, восстанавливается
командой `init` плюс повторным прогоном. Итоговые прогнозы, когда появятся, нужно экспортировать
в CSV или Parquet и коммитить — именно их будет смотреть проверяющий.

**Мощность нормализована в [0, 1].** Это следует из данных: 101 различное значение, шаг 0.01.
Хранилище отвергает значения вне этого диапазона — и для прогноза, и для факта.

**`lead_hours` не передаётся, а вычисляется** из `target_ts - issued_at`. Рассогласовать горизонт и момент
выпуска невозможно. Прогноз в прошлое и выход за объявленный горизонт отклоняются.

**Февраля 2026 в исходных данных нет.** Файлы названы по 28.02.2026, но заканчиваются 2026-01-31 23:50.
Значит факта для тестового периода у нас нет, и посчитать по нему качество нельзя.
Январь 2026 полный — он и годится как отложенная выборка. В README это надо сказать прямо,
а не изображать оценку там, где измерить нечем.

**Не заполнять пропуски нулями.** У турбины 1 отсутствует 6.56% меток, крупнейший разрыв — с 18.05.2024
по 28.06.2024. Причина неизвестна: телеметрия, обслуживание или отбор записей. Нулевая мощность
в этих местах научит модель неправде.

**Турбину определяет имя файла, а не порядок в каталоге.** Исходные имена содержали
идентификаторы Google Drive, и `sorted(glob("*.csv"))` ставил турбину 2 первой: имя начиналось
с `1_`, а `_` в ASCII меньше `h` из `1hub`. Код, нумерующий файлы по порядку, связал бы координаты
турбины 1 с данными турбины 2 — площадки в 300 м друг от друга, ряды коррелируют на 0.96,
такую ошибку потом не заметить. Исправлено 13:29: файлы переименованы, `audit_data.py` читает
номер турбины из имени (`turbine_id()`), сортирует отчёт по нему и отказывается работать,
если номер не распознан или два файла заявляют одну турбину.
**Новые файлы данных именовать `turbine_<N>.csv`** — иначе аудит остановится с объяснением.

**Часовой пояс данных не определён.** В CSV меток пояса нет. До уточнения у организаторов не назначать UTC
автоматически: выбор границы часа меняет и соответствие с погодой, и доступность последнего наблюдения.

---

## Хронология

### 17:00 — Помощник Windcast на ASTRA и подключение NVIDIA API (Codex)

**Название:** Windcast — проект, RentBox — команда. Исправлены заголовок README,
название API и меню запуска; технические пути и переменные RENTBOX сохранены.

**Помощник сайта:** кнопка «Помощник» открывает чат с OpenAI `gpt-6-astra` через
Responses API. Добавлены системный промпт, справка по реальным разделам, контекст
экрана, чтение выбранного выпуска из backend и источники ответа. Structured Outputs
и серверная валидация ограничивают кнопки переходами и CSV. История — в памяти
браузера; ключ — на сервере. Без ключа показывается явно обозначенная обычная
справка. Ошибочная формулировка о процентах от номинала убрана из справки и CLI.
Интеграция перенесена на одновременно слитый dashboard v2: контекст включает
язык и режим симуляции. Синтетические сценарии не получают источники или CSV
сохранённого прогноза. Правила симуляции описаны в отдельной статье справки.

**NVIDIA:** конфигурация NIM, проверочный endpoint, пояснения готового прогноза
и CLI рекомендаций по результатам отбора ноября–декабря. NVIDIA API не заменяет
обучение CPU/CUDA/Brev. `train.sh` находит подготовленный Python, учитывает порядок
флагов, сохраняет новые модели отдельно и завершает выбранный набор кандидатов.
При возобновлении проверяются хэши данных и параметры кандидатов.

**Для команды:** новые маршруты `/api/help/status`, `/api/help/chat`,
`/api/integrations/nvidia`, `/api/integrations/nvidia/check`,
`/api/agent/runs/{run_id}/explanation`; контракт и OpenAPI обновлены.
Инструкции: `docs/AI-HELP.md`, `docs/NVIDIA.md`, настройки — `.env.example`.

**Проверки:** 21 тест NVIDIA/финализации обучения проходит; Ruff для новых Python
модулей, TypeScript и ESLint для frontend проходят. `./run.sh all` поднял сайт на
3000 и API на 8001; через прокси сайта получены статус помощника, ответ справки
с кнопкой CSV и исходное пояснение прогноза без NVIDIA. RTX 5090 определяется,
флаги `--check --cpu` и `--gpu --check` выбирают нужный режим без обучения.
Ключи OpenAI/NVIDIA не заданы,
поэтому реальные ответы внешних моделей ещё не проверены. Численная модель,
исторические границы данных и правила доступности погоды не менялись.

### 16:25 — Исправлен запуск дашборда через run.sh (Codex)

- Воспроизведён сбой пункта 2 меню: старый Next.js занимал lock на порту
  3010, зависимость `three` отсутствовала, а прокси обращался к прежнему
  backend на 8051. HTTP 500 ошибочно считался готовым интерфейсом.
- `run.sh` завершает предыдущий Next.js только после проверки команды
  и рабочей директории процесса; новый процесс запускается отдельно от терминала.
- Зависимости синхронизируются через `npm ci` при смене manifest/lock-файла
  или отсутствии нужных модулей. При повторном запуске установка пропускается.
- Порт существующего Compose-backend сохраняется; адрес демо формируется
  после выбора порта. Ошибки Docker, процесса и HTTP больше не скрываются,
  готовность дашборда требует ответа страницы и проксируемого API.
- Выполнены запуск и повторный `./run.sh all`, проверены HTTP 200 страницы,
  `/api/health` и истории запусков. Текущие адреса: UI 3000, backend 8001.
  Доступ к странице и API дополнительно проверен из Windows через localhost.
  `bash -n` и `git diff --check` проходят; автотесты не запускались.

### 16:20 — Чистая сборка после NOAA проверена, запуск через меню (Claude Code)

**Проверено то, что висело незакрытым в [REQUIREMENTS.md](docs/REQUIREMENTS.md):**
клон в пустой каталог, `./run.sh demo` — сборка образа, health, запуск агента,
**96 почасовых значений получены**. Переход на NOAA GFS с 730 циклами GRIB
и ecCodes сборку не сломал. Клон весит 85 МБ, ключи и GPU не нужны.
Пункт в требованиях отмечен выполненным.

**Добавлено меню:** `./run.sh` без аргументов показывает шесть пунктов, выбор цифрой,
Enter выбирает первый. Прямые команды (`demo`, `all`, `check`, `logs`, `stop`)
работают как прежде; в скриптах и CI меню не показывается — проверка `[ -t 0 ]`,
чтобы автоматизация не зависла на вводе.

### 16:05 — Уточнения цифр и команд в README (Codex)

- Сохранена структура README команды. Снижение январского MAE на 53,5%
  описано относительно того же набора прогнозных строк; единицы ошибки
  привязаны к исходной нормализованной шкале.
- Недовыработка и её стоимость помечены как сценарная оценка. Диапазон
  для 226 МВт·ч и 25–30 ₸/кВт·ч исправлен на 5.65–6.78 млн ₸ в README
  и бэклоге; признаки возможных простоев требуют подтверждения причин.
- Пример CSV обновлён по текущей модели NOAA. Уточнено, для каких данных
  сохранены SHA-256: погодные CSV, индексы и извлечённые GRIB-поля.
- Убраны фиксированные обещания времени запуска; исправлены команды
  просмотра вклада и инструкция настройки существующего `.env`.
- После параллельных коммитов команды актуализированы статус подключения
  дашборда, меню запуска и запись о проверке Docker от Claude Code.
- Проверены 30 локальных ссылок, 6 якорей и `git diff --check`;
  четыре строки примера совпадают с сохранённым прогнозом NOAA.
  Изменена только документация; приложение и тесты не запускались.

### 15:54 — Подключение Windcast к реальному backend (Codex)

**Изменено:** HTTP-клиент и управление состоянием frontend; запуск расчёта,
опрос агента, график, события, предупреждения, источники каждого часа и серверный CSV.
Синтетический генератор не вызывается интерфейсом. Факт февраля, ошибки,
интервалы и невыданная числовая погода отображаются как отсутствующие.

**История:** GET /api/agent/runs?limit=100 читает состояния из DuckDB;
история переживает перезагрузку страницы, открытие восстанавливает параметры
и результат. Незавершённый запуск можно продолжить опрашивать без нового POST.

**Запуск:** ./run.sh all передаёт RENTBOX_API_URL для Next.js-прокси /api.
Браузер использует свой origin; отдельная настройка CORS не нужна. Сохранены
3D-сцена и связь её мощности с выбранным часом; отсутствующий ветер не подменяется.

**Контекст:** обновлены README, docs/API.md, docs/FRONTEND.md,
docs/frontend-integration.md и AGENTS.md, экспортирован OpenAPI.
Временные настройки CSV остаются исследовательскими.

**Проверки:** TypeScript, ESLint, Ruff и git diff --check. Автотесты,
браузерный сценарий и Docker в рамках этой интеграции не запускались.

### 15:31 — README с понятным запуском и текущим состоянием (Codex)

- В начало вынесены назначение, требования к окружению, команда запуска и
  ожидаемые 96 строк прогноза. Добавлены навигация и таблица возможностей.
- Описаны смысл `as_of`, шаги агента, проверка исторической публикации,
  единицы метрик и отличие январской оценки от февральского прогноза.
- Статус дашборда сверен с последним frontend-коммитом: 3D-турбина есть,
  данные пока синтетические, интеграция с API не завершена.
- Добавлены схема архитектуры, карта файлов и пример запроса API.
  Подробности обучения и лицензии свёрнуты в раскрывающиеся блоки;
  раскрытие внешних материалов и временные допущения сохранены.
- Команды сверены с `run.sh`, Makefile и конфигурацией. Проверены 25 локальных
  ссылок, 5 якорей и `git diff --check`. Запуск приложения и тесты в рамках
  редактирования документации не выполнялись.

### 15:16 — Основная модель NOAA, CPU-экспорт и полный replay (Codex)

- Завершены 400 конфигураций: 40 CPU, 180 RTX 5090, 180 Brev A6000.
  По ноябрю–декабрю выбран ансамбль пяти MLP; январская MAE **0.15947**,
  RMSE **0.23190**. Январь — мониторинг разработки; февральского факта нет.
- `artifacts/gfs-model/`: веса для CPU, отдельная январская модель, метрики,
  персистентные базовые модели, 29 февральских выпусков и краткий отчёт.
  `artifacts/gfs-search/`: протокол и результаты всех 400 конфигураций.
- `data/gfs-runs/`: все 730 циклов NOAA, URL и исходные даты публикации,
  ETag, диапазоны байтов, SHA-256. Каждый исходный объект доступен не позже
  своего исторического `as_of`. Время CSV всё ещё не подтверждено.
- CLI теперь выбирает модель NOAA. Агент уже выбирает её автоматически;
  API-replay завершён: **29/29**, 2784 значения. Смена модели создала ревизию,
  одинаковый повтор переиспользовал расчёт (`reports/agent-noaa-*.json`).
- Docker включает новую модель, архив и загрузчик. ecCodes закреплён в
  backend/uv.lock; изменяемый погодный кэш вынесен в `/app/state/gfs`.
  В `.gitattributes` отключена нормализация байтов погодных CSV для сохранения SHA.
- Ruff и `git diff --check` выполнены. Тесты не запускались. Новый прогноз
  фактически рассчитан через CLI и API; Docker после этой смены нужно
  повторно проверить перед сдачей (предыдущие проверки Claude см. ниже).
- Для фронта актуальны `artifacts/gfs-model/metrics.json` и пример
  `reports/agent-noaa-first-forecast.json`. В `weather.sources` — происхождение,
  погодные числовые признаки в API-точки пока не включены. Факта февраля и
  вероятностных интервалов нет; рисовать их как измеренные нельзя.

### 16:05 — Стенд для интеграции фронта и backend (Claude Code)

**Добавлено:** `./run.sh all` поднимает backend и дашборд вместе,
[docs/frontend-integration.md](docs/frontend-integration.md) — что где лежит и как связать,
[docs/samples/](docs/samples/) — пять живых ответов API, снятых с работающего сервиса
(health, turbines, data/summary, завершённый запуск с событиями, прогноз на 48 ч).

**Проверено:** `npm install`, сборка `turbo build --filter=web` за 6.5 с, оба сервиса
поднимаются одной командой и отвечают. CORS уже разрешает `localhost:3000`.

**🔴 Расхождение контракта и вёрстки, которое надо решить до графика.** Тип `ForecastPoint`
в `apps/web/lib/forecast-data.ts` ожидает девять полей, backend отдаёт три из них:

- `forecast` ← `predicted_power`, `timestamp` ← `valid_time` — просто переименование;
- **`actual` не существует** — февральской телеметрии нет, данные обрываются 31.01.
  На февральских графиках линию факта рисовать нечем; для января факт есть
  в `artifacts/ensemble/january_predictions.csv`;
- **`lower` и `upper` не реализованы** — интервал неопределённости это пункт 2.2 бэклога.
  Либо убрать полосу с графика, либо дождаться квантильной модели;
- `wind` и `temperature` лежат в `series[].weather`, а не в точках.

**Полезное для фронта:** `analysis.warnings` в ответе `/forecast` — готовый текст для баннера,
там уже приходят содержательные вещи вроде «Расхождение GFS/ICON по ветру достигает 9.2 м/с»
и «Прогнозируется изменение мощности до 0.57 за час». `events[]` в статусе запуска — готовая
лента шагов агента, `stage` и `progress` — для прогресс-бара.

### 15:40 — Запуск одной командой и починка целостности данных (Claude Code)

**Добавлено:** `run.sh` и раздел «Быстрый старт» в README. `./run.sh demo` поднимает
сервис и считает прогноз на 48 часов: проверяет окружение, создаёт `.env`, собирает
образ, ждёт готовности, запускает агента, сохраняет 96 значений в
`artifacts/demo-forecast.csv`. Ещё есть `check`, `logs`, `stop`. Без Docker переходит
на локальный запуск через uv, занятый порт обходит.

**🔴 Найдено и исправлено на чистом клоне: проект не работал у проверяющего.**

`core.autocrlf=input` переводил CRLF в LF при коммите, и в клоне исходные CSV
оказывались на 142360 и 149499 байт короче — ровно по числу строк. SHA-256
переставали совпадать с `reports/data-audit.json`, и запуск агента падал:
`DATA_QUALITY_ERROR: Исходные данные изменились; требуется новый аудит`.

Добавлен `.gitattributes` с `data/incoming/*.csv -text`, файлы перекоммичены.
Теперь git отдаёт их байт в байт, хеш совпадает, сквозной сценарий из клона проходит.

**Вторая находка:** в README было написано, что `.env` необязателен. Сервис
действительно поднимается, но расчёт возвращает `CONFIGURATION_REQUIRED` — не заданы
настройки времени. Теперь это указано явно, с готовыми командами; `run.sh` создаёт
`.env` сам и помечает настройки как исследовательские.

**Проверено дважды из чистого клона:** сборка образа, health, запуск агента,
96 почасовых значений с происхождением каждой строки, выгрузка CSV. Без ключей
и без интернета после сборки — архив погоды лежит в репозитории, 109 файлов.

### 14:58 — Сохранены результаты расширенного обучения (Codex)

- `artifacts/expanded/`: веса, январские прогнозы и метрики, полный февральский replay.
  Ансамбль объединяет модели RTX 5090 и A6000, выбранные по ноябрю–декабрю
  среди 1204 конфигураций (включая CPU и CatBoost).
- Валидация: MAE 0.16261. Январь: MAE 0.16945, RMSE 0.24986;
  1–24 ч MAE 0.15966, 25–48 ч 0.17956. Снижение MAE против первого ансамбля
  на январе не получено; это отражено без подмены метрик.
- `artifacts/search/winter-validation.json`: ещё 18 кандидатов оценены
  на ноябре–декабре 2024 с обучением до этого периода.
- `scripts/finalize_search.py --expanded --output /полный/путь/artifacts/expanded`
  воспроизводит выбор и экспорт на CPU. Каталог `artifacts/ensemble/` сохраняет
  первый выпущенный ансамбль. NOAA-модель обучается и будет отдельным артефактом.

### 14:56 — Агент подключён, переход на исходные циклы NOAA (Codex, работа продолжается)

- `src/agent.py:create_agent` реализует policy-контроллер через согласованный
  интерфейс backend. Получает погоду, проверяет историю/время, считает на CPU,
  анализирует результат; backend сохраняет выпуски и ревизии в DuckDB.
- Первый полный replay через HTTP завершён: 29/29, 2784 точки. После получения
  контракта v0.3 адаптер переведён на `weather.sources` / `weather_inputs`;
  выполняется повторный реальный replay. Прототипная локальная БД v0.2 сохранена
  как `data/forecasts-v02.duckdb`; текущая создаётся заново, числовые результаты
  уже сохранены в CSV и отчёте.
- **Важное исправление статуса:** ансамбль давно закоммичен (`913adf2`), агент
  теперь существует. Старые записи ниже про отсутствие этих файлов устарели.
- Исследовательский запуск `make demo` явно задаёт UTC+5 / interval_start и
  `RENTBOX_ALLOW_RESEARCH_TIME_SETTINGS=true`; `confirmed=false` сохраняется.
  Не выставлять подтверждение организаторов ради обхода проверки.
- Получены все 29 февральских циклов NOAA GFS 12 UTC: каждый исходный GRIB-объект
  опубликован до решения 18 UTC по S3 Last-Modified. `scripts/fetch_gfs_runs.py`
  сохраняет цикл, диапазоны байтов, хэши, ETag, дату публикации и извлечённые точки.
  На Brev скачивается такая же история с марта 2024 для отдельного обучения.
  **Не смешивать её с Previous Runs под видом одинакового источника.**
- `docs/FORECAST_PROTOCOL.md` — ограничения без будущей информации для всего
  решения. Previous Runs остаётся исследовательским источником с оценкой +12ч.
  NOAA-путь агентом уже поддержан; переключение произойдёт после сохранения
  отдельно обученного `artifacts/gfs-model/model.joblib`.
- По 1204 конфигурациям выбран расширенный ансамбль: Nov/Dec MAE 0.16261;
  январская MAE 0.16945, RMSE 0.24986. Дополнительный перебор почти исчерпал пользу.
  Основное улучшение теперь — более свежие выпуски с доказанной доступностью.
- **Координация:** Codex завершает NOAA-архив, `train_gfs.py`, модель и интеграцию
  агента. Просьба сохранять изменения коллег отдельными явными путями; не
  коммитить незавершённый чужой код через `git add -A` / `git add -u`.

### 15:05 — Поиск моделей на RTX 5090 исчерпан (Claude Code)

**Добавлено:** `scripts/train_rtx5090.py` (плоский поиск), `scripts/evolve_rtx5090.py`
(поколения), `scripts/finalize_rtx5090.py` (сборка ансамбля и январь один раз).
Результаты — `artifacts/rtx5090-local/`. Скрипты Codex не тронуты: его `finalize_search.py`
проверяет фиксированный состав трёх воркеров и эти результаты отверг бы.

**Прогоны:**

| Поиск | Кандидатов | Лучший на валидации |
|---|---|---|
| плоский, 8 шардов | 600 | 0.16398 |
| эволюция, 6 поколений | 384 | 0.16372 |
| ансамбль top-10 из обоих | — | **0.16308** |

Эволюция остановилась сама по плато: три поколения подряд прирост меньше 0.15%.
Медиана популяции шла 0.17428 → 0.16617, то есть отбор работал. Пик карты 94%, 4.5 ГБ из 24.

**Главный результат — отрицательный, и он важнее моделей.** Ансамбль с лучшей валидацией
(0.16308) на январе дал **0.17010 против 0.16933** у ансамбля Codex — то есть на 0.5% хуже.
Это переподгонка под ноябрь-декабрь. 984 кандидата и шесть поколений отбора не улучшили
результат на отложенном месяце.

**Вывод: перебор моделей исчерпан, узкое место не в них.** Дальше имеет смысл только то,
что меняет вход или постановку: исключение простоев из обучения, веса погодных моделей,
коррекция январского смещения. И agentic-цикл, который по требованию 4 всё ещё не закрыт.

**Что ещё выяснилось из 984 прогонов:**

- `point`-признаки стабильно бьют `trajectory` (медиана 0.16720 против 0.17260), и весь топ — `point`.
  49 дополнительных признаков контекста только мешают.
- MLP доминирует: 0.16372 против 0.17171 у бустинга и 0.18148 у ExtraTrees. Эволюция вытеснила
  остальные семейства сама — в элите последних поколений только MLP.
- Архитектура почти не важна: лучшая сеть `128-64-32`, самая маленькая в сетке, а `1024-512-256`
  на пятом месте. Топ-10 укладываются в 0.49%.
- Коррекция смещения на валидации бесполезна: отобранные офсеты порядка 1e-4. Январский
  bias +0.06 возникает именно на январе и константой с валидации не лечится.

**Что в репозитории:** `summary.json`, `run.json`, `leaderboard.csv` обоих поисков, `best.joblib`,
`champion.joblib` и собранный `ensemble/`. Промежуточные кандидаты (984 × 3 файла) — в `.gitignore`,
поиск воспроизводится из `GRID_SEED`.

### 14:40 — Трассировка требований кейса (Claude Code)

**Добавлено:** [docs/REQUIREMENTS.md](docs/REQUIREMENTS.md) — шесть дословных требований задачи,
где каждое закрыто в коде, и три ограничения, которые нельзя нарушать.

**Проверено по коду, а не по памяти:**

- Архивные прогнозы вместо факта — соблюдается. `src/weather.py` тянет Open-Meteo Previous Runs
  (`*_previous_day{N}`), реанализ не используется, доступность ограничена
  `valid_time − offset·24ч + 12ч ≤ as_of`, нулевой офсет исключён.
- Воспроизведение тестового периода — закрыто. `february_replay.csv`: 29 выпусков
  с 31.01 по 28.02, горизонты 1–48 ч, обе турбины, 2784 строки.
- Получение погоды по координатам — закрыто, `fetch_month` с повторами и кэшем.

**Не закрыто — требование «Agentic AI, полный цикл» (пункт 4).** Шаги «анализ результата»
и «повторный расчёт при обновлении входных данных» не реализованы, `src/agent.py` нет.
Сейчас это конвейер, запускаемый человеком, а кейс требует цикла. `ForecastStore` под это
готов — `revisions()` и поле `trigger` со значением `weather_update`, — но прогнозы пишутся
только в CSV, история пустая.

**Расхождение между локальным запуском и клоном.** Из чистого клона `predict.py` работает,
но берёт `artifacts/forecast/model.joblib` (`boost_both_absolute_error`): каталог
`artifacts/ensemble/` не в `.gitignore`, он просто не закоммичен. Локально используется
ансамбль `top_3_mean`. Пока это так, заявленные метрики у проверяющего не воспроизведутся.

### 14:40 — Погодные метаданные Previous Runs в backend (Codex)

**Изменено:** контракт v0.3, `schemas/weather.py`, схемы прогноза, адаптер DuckDB
и CSV. У серии есть каталог погодных ответов, у каждого часа — ссылки на GFS/ICON
с выбранным offset и оценкой доступности. Источники разделены по product на
single_run/previous_runs. Неизвестные время выпуска и публикации остаются null.

**Проверки в коде:** offset 1…7, известная политика previous_runs_offset_plus_12h_v1,
пересчёт valid_time - offset*24h + 12h и проверка <= as_of; неизвестные/повторные
ссылки и повтор модели в одном часу отклоняются. Предупреждение об оценочной
доступности добавляется в журнал и ответы независимо от текста агента.

**Хранение и совместимость:** weather_run=NULL для Previous Runs и нескольких
источников, полные метаданные — в api_runs.result. Старые одноисточниковые ответы
нормализуются при чтении без перезаписи базы. Сравнение повторных результатов
не зависит от source_id, порядка источников и времени повторного скачивания.
CSV сохраняет одну строку на прогнозный час; добавлены оценка, основание и
weather_inputs_json со всеми источниками.

**Передача контекста:** обновлены docs/API.md и docs/backend/agent-integration.md;
примеры, отображение на фронте и соответствие src/weather.py — в
docs/backend/weather-provenance.md. Код модели, погоды и ForecastStore не менялся.
Политика +12 часов сама по себе не подтверждает историческое время публикации.

**Проверки выполнения:** OpenAPI обновлён; Ruff и git diff --check прошли.
Тесты и Docker в рамках этой задачи не запускались; реальный агент всё ещё
требует подключения. Контекст backend в AGENTS.md и ограничения в README
обновлены под контракт v0.3.

### 14:31 — Ансамбль и расширенное обучение на двух GPU (Codex)

- Первый поиск завершён: 40 CPU + 24 RTX 5090 + 24 A6000. По ноябрю–декабрю
  выбран ансамбль трёх MLP; январская MAE снизилась с 0.19420 до 0.16933.
- Готовые веса, январские прогнозы и 29 выпусков февраля — `artifacts/ensemble/`.
  По умолчанию `scripts/predict.py` использует этот ансамбль; инференс на NumPy.
- Базовые прогнозы января: последний известный полный час MAE 0.34268;
  последние доступные сутки MAE 0.36480. Скрипт `scripts/evaluate_baselines.py`.
- По прямому запросу пользователя расширено обучение: Claude завершил 600
  локальных конфигураций и ведёт эволюционный поиск; Codex завершил ещё 480
  конфигураций на A6000, запускает 36 GPU CatBoost. Данные поиска те же, с SHA-256;
  январь и февраль не участвуют в подборе параметров. Январь уже является месяцем
  мониторинга разработки, а не новым нетронутым тестом.
- **Координация:** Codex берёт `src/agent.py`, `src/api.py`, интеграцию с DuckDB
  и сборку итогового ансамбля. Локальные `train_rtx5090.py` / `evolve_rtx5090.py`
  остаются за Claude. Не расширять перебор ценой рабочего цикла по ТЗ.
- Точное время публикации Previous Runs и часовой пояс CSV остаются ограничениями;
  их нельзя выдавать за подтверждённые метаданные.

### 14:04 — Прогнозная модель и обучение на Brev (Codex)

Сохранены месячные архивы GFS/ICON за январь 2024 — март 2026 с хэшами.
Первая модель выбрана по декабрю, январский MAE 0.19420 против 0.30240
у среднего по турбине. Рассчитаны 29 выпусков с 31 января до 28 февраля,
2784 строки; мартовский хвост отмечен `in_february=false`.

CLI `python -m scripts.predict --as-of 2026-01-31T18:00:00Z --horizon 48`
фактически вернул 96 строк из кэша, без сети и GPU. Ruff и проверка
diff/шаблонов секретов прошли; тесты хранилища не менялись.

Расширенный поиск: завершены 40 CPU-конфигураций и 24 MLP на RTX 5090.
Выбор идёт по ноябрю–декабрю; январские цели в пакет workers не входят.
Создан `wind-mlp` в Brev: A6000 48 ГБ, `massedcompute_A6000_plus`, $0.684/ч.
GPU подтверждена через nvidia-smi; готовится отдельная серия из 24 MLP.
Первый прогноз остаётся в `artifacts/forecast`; новый ансамбль будет отдельно.

**Уточнение к пункту про простои ниже:** для прогноза фактической выработки
нулевые значения при простое нельзя автоматически выкидывать из целевой
переменной: это меняет задачу на потенциальную выработку. Такой фильтр можно
сравнить на валидации для физической кривой, но не применять без оценки ко
всему прогнозному обучению. Сейчас эти реальные наблюдения сохранены.

### 14:10 — Бэклог отраслевых сценариев и находки по простоям (Claude Code)

**Добавлено:** [docs/BACKLOG.md](docs/BACKLOG.md) — 17 пунктов сверх PLAN.md, разбитых
на отраслевые сценарии, улучшения прогноза и поведение агента. У каждого оценка времени,
зависимости и статус. Порядок на случай нехватки времени указан в конце файла.

**Проверено на данных и вынесено в бэклог как факты:**

- **Смены режима турбин нет.** Кривая мощности Т1 до и после разрыва 18.05–28.06.2024
  совпадает, средний сдвиг `+0.001`. Отношение Т1/Т2 по месяцам без тренда. Деградацию
  искать не нужно — это закрытая гипотеза, не тратьте на неё время.
- **Есть эпизодические простои, и отношение выработки двух турбин их детектирует.**
  Турбины в 300 м видят один ветер, поэтому отношение не зависит от погоды: проседает,
  когда стоит первая, растёт — когда вторая. Июнь 2024: Т1 простаивала 22.2% времени
  при рабочем ветре, Т2 — 0%. Февраль 2025 наоборот: Т2 6.9%, Т1 0.9%.
- **Цена простоев:** Т1 — 2.2% записей и 1.7% недовыработки, Т2 — 1.5% и 1.1%.
  Суммарно ~226 МВт·ч в год при номинале 2.5 МВт, это 5.7–8.1 млн ₸ в год
  при тарифе 25–30 ₸/кВт·ч.

**Что из этого влияет на модель прямо сейчас:** часы простоя (ветер 6–12 м/с, мощность 0)
нельзя оставлять в обучающей выборке — модель научится занижать прогноз при хорошем ветре.
У Т1 это 2.2% в среднем и 22% за июнь 2024. Пункт 2.3 бэклога, ~10 минут работы.

**Самое дешёвое из бэклога:** 2.1 — персистенция как базовая планка, 15 минут. Без неё
любое значение MAE не с чем сравнить.

### 14:00 — Backend дашборда по контракту команды (Codex)

**Добавлено:** `src.api:app`, маршруты `/api` из `docs/API.md`, реальные координаты,
сводка аудита с проверкой SHA-256, очередь фоновых запусков, статусы и события,
выдача JSON/CSV, последовательный replay. Контракт OpenAPI экспортирован.

**Хранение:** существующий `ForecastStore` используется без изменения его кода.
В тот же файл DuckDB добавлены `api_runs`/`api_replays` для состояний и метаданных.
Точки обеих турбин и завершение задания сохраняются одной транзакцией.
Один процесс Uvicorn владеет БД; запросы к соединению сериализованы lock.
После перезапуска незавершённые задания получают `JOB_INTERRUPTED`.

**Интеграция:** модуль агента реализует `ForecastEngine.run(request, context)`;
интерфейс и правила — в `docs/backend/agent-integration.md`.
Backend владеет записью результатов, поэтому агенту не нужно писать их повторно.
`RENTBOX_AGENT_FACTORY` подключает фабрику; до её реализации расчёты недоступны.
Время CSV требует явных настроек и подтверждения; предположение UTC не сделано.

**Контроль результата:** полный горизонт 24/48 часов, диапазон 0–1, соответствие
турбин, доступность погоды и обучающих целей к `as_of`. Хеш всех входов связывает
повторное использование или новую ревизию. Февральские метрики — `null`.

**Состояние проверок:** OpenAPI сгенерирован, Ruff выполнен. Существующие тесты
адаптированы без прогона; Docker и полный сценарий с реальным агентом не проверены.
Первый коммит основной части backend разрешён пользователем после реализации.

### 13:44 — Первое обучение на CPU и RTX 5090 (Codex, работа продолжается)

**Добавлено:** `src/data.py`, `src/model.py`, `scripts/train_power_curve.py`.
Агрегация использует только полные часы (6 измерений), без заполнения пропусков.
Часовой пояс CSV не назначается. Для первого эксперимента метка 10 минут
считается началом интервала; это ещё не подтверждённая семантика.

**Обучены:** монотонная кривая, HistGradientBoosting, ExtraTrees, MLP на CUDA.
Выбор по декабрю 2025, январь 2026 — отдельная проверка. В этом опыте победила
MLP: январский MAE 0.02493. **Это ошибка преобразования фактического ветра
в мощность, НЕ качество прогноза на сутки вперёд.** Нельзя показывать эту
цифру на дашборде как качество погодного прогноза. Артефакт экспортирован
с NumPy-весами, для расчёта GPU/PyTorch не требуются.

**Погода:** у Open-Meteo Single Runs старые ECMWF обозначены как hindcast;
историческая доступность этих выпусков не подтверждена. Исследуется архив
GFS Previous Runs с фиксированными заблаговременностями 1–3 дня, с явным
ограничением доступности относительно `as_of`. Сопоставление с CSV требует
подтверждения зоны или явно помеченного исследовательского предположения.

### 13:29 — План агента и контракт API (Codex)

**Добавлено:** `docs/PLAN.md`, `docs/API.md`; README дополнен текущим состоянием
и ссылками. Это проект интерфейса, маршруты ещё не реализованы.

**Разделение работ:** Codex — `src/data.py`, `src/weather.py`, `src/model.py`,
`src/agent.py`, `src/api.py`; Claude Code — существующее хранилище `src/storage.py`;
разработчики интерфейса — `frontend/`. Перед расширением чужого интерфейса
сверяться с журналом. Готовое хранилище используется через `ForecastStore`.

**Для фронта:** номера турбин — целые `1` и `2`; одно задание API может охватывать
обе турбины. Его строковый `run_id` отличается от числовых ID отдельных выпусков
в DuckDB — последние передаются как `storage_run_id` в каждой серии.

**Для интеграции:** API принимает время с зоной; адаптер нормализует его в UTC,
передаёт в текущий `TIMESTAMP` без `tzinfo` и возвращает в JSON с `Z`.
Это не назначает часовой пояс исходным CSV: его ещё нужно подтвердить.
`as_of` запроса соответствует `issued_at`, а не фактическому времени запуска.

**Замечание по метрикам:** текущий `accuracy_by_lead()` использует
`latest_forecasts` и оценивает последний выпуск на час. Для сравнения всех
ежедневных прогнозов на 24/48 часов нужен отдельный запрос по всем выпускам.

**Погода:** один запрос Open-Meteo Single Runs (`ecmwf_ifs`, цикл
`2026-01-31T12:00`) вернул 72 почасовые точки для координат ВЭС. Перед
использованием требуется проверить происхождение выпуска и время его
исторической доступности, включая задержку публикации. Примеры `previous_day1`
в интерфейсе хранилища не определяют будущий выбор погодного API.

### 13:29 — Турбины больше нельзя перепутать (Claude Code)

**Изменено:** `data/incoming/` переименованы в `turbine_1.csv` и `turbine_2.csv` (через `git mv`,
содержимое не тронуто — SHA-256 в отчёте прежние). `scripts/audit_data.py`: добавлена `turbine_id()`,
отчёт сортируется по номеру турбины, а не по имени файла; в каждую запись JSON добавлено поле
`turbine_id`; вывод в консоли называет турбину. `reports/data-audit.*` перегенерированы.

**Зачем:** `sorted(glob("*.csv"))` возвращал турбину 2 первой, и в `data-audit.json` турбины
лежали в обратном порядке без каких-либо признаков этого. Любой код, берущий файлы глобом
и нумерующий их по позиции, привязал бы координаты одной турбины к измерениям другой.

**Что это меняет для других:** обращаться к данным по именам `data/incoming/turbine_1.csv`
и `turbine_2.csv`; в `reports/data-audit.json` записи теперь упорядочены по `turbine_id`
и содержат это поле явно — полагаться на позицию в списке не нужно.

### 13:25 — Хранилище истории прогнозов (Claude Code)

**Добавлено:** `src/storage.py`, `tests/test_storage.py` (10 тестов, проходят), `src/__init__.py`.
`data/*.duckdb` внесены в `.gitignore`.

**Зачем:** ТЗ требует цикла, в котором система пересчитывает прогноз при обновлении входных данных
и анализирует результат. Для этого нужна история: какой прогноз на какой час был выдан, когда,
на каком выпуске погоды, и как он менялся. Схема хранит каждую ревизию, а не только последнюю.

**Что это даёт другим частям проекта:**
- цикл агента пишет результат через `record_run` + `record_forecast`;
- шаг «анализ результата» читает `revisions()` — видно, куда и на сколько уехал прогноз;
- валидация на январе делается через `record_actuals` + `accuracy_by_lead()`;
- на Demo Day есть что показать SQL-запросом.

**Что нельзя ломать:** `issued_at` — это симулируемый момент принятия решения (например, 31 января 12:00),
а не время запуска скрипта. Если туда попадёт `datetime.now()`, весь бэктест потеряет смысл.

**Исправлено по ходу:** команды `summary` и `accuracy` на несуществующей базе молча создавали пустой файл
и печатали нули. Теперь они открывают базу только на чтение и сообщают, что её нет и как создать.

### 13:05 — Аудит исходных данных (Codex)

**Добавлено:** `scripts/audit_data.py`, `reports/data-audit.md`, `reports/data-audit.json`,
исходные CSV в `data/incoming/`. Коммит `87498b7`.

**Зачем:** прежде чем обучать модель, зафиксировать, что в данных фактически есть,
и записать SHA-256, чтобы потом можно было доказать, что работали с тем же файлом.

**Ключевые факты из отчёта:** шаг измерений 10 минут; период 2023-03-11 — 2026-01-31;
пропущено 6.56% меток у турбины 1 и 1.87% у турбины 2; дубликатов и пустых значений нет;
мощность в [0, 1] с шагом 0.01; координаты турбин 43.645150/78.535604 и 43.643198/78.538828.

---

## Открытые вопросы

| Вопрос | Кому | Блокирует |
|---|---|---|
| Часовой пояс данных и граница часа (левая или правая) | организаторы | сопоставление с погодой |
| Цель: две отдельные мощности или суммарная выработка ВЭС | организаторы | формат результата |
| Официальная метрика и формат сдачи прогноза | организаторы | приёмка |
| Время ежедневного выпуска прогноза | организаторы | расписание агента |
